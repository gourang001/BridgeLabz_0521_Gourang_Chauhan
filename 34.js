// 34. Create a function that returns an array with words inside a text.

function solve(text){
    let arr =text.split(" ");
    return arr;
}

let text="hello i am going to college"
console.log(solve(text));
